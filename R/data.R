# Documentation for data files
.pkgenv <- new.env(parent = emptyenv()) # not exported

#' Monte Carlo runs used for Sensitivity Analysis and GAM model training.
#'
#' Columns represent input parameter variations for which a model has been
#' or is to be run. Currently generated by `draw_psa_runs`.
#'
#' Names are expected to match `input_list("basic")`. The table is used to set
#' soft bounds on basic inputs.
#'
#' @name PSA_config
#' @format  A data frame with one column per PSA parameter, and one row per MC draw.
#' @source `draw_psa_runs`
data("PSA_config", envir = .pkgenv)

#' Model input configuration table
#'
#' Defines (dynamic) input types, limits, description, etc. generated by
#' `dev/parse_ui_table.R` and modified by "data-raw/load_soft_bounds.R"
#'
#' @name input_config_table
#' @format  A data frame with one row per model input.
#' \describe{
#'   \item{group}{defines tab/section where input is placed}
#'   \item{id}{unique input identifyer}
#'   \item{description}{ input description }
#'   \item{default}{ default value }
#'   \item{type}{"numeric", "slider", "checkbox", "text", "matrix", ... }
#'   \item{basic}{ boolean, is the input included in GAM model?}
#'   \item{fixed}{ boolean, should the input be available for advanced users?}
#'   \item{rel_min}{ soft minimum (set by `load_soft_bounds`) }
#'   \item{rel_max}{ soft maximum (set by `load_soft_bounds`) }
#'   \item{abs_min}{ hard minimum (read from `input_config.tsv`) }
#'   \item{abs_max}{ hard maximum (read from `input_config.tsv`) }
#'   \item{step}{ used for numeric & slider inputs }
#'   \item{unit}{ used to allow conversion between PSA variables and UI fields, see `utils.R/parse_units` }
#'   \item{notes}{ currently unused }
#'   \item{valid}{ boolean, has the input row been parsed successfully?}
#'   ...
#' }
#' @source `dev/parse_ui_table.R`, `data-raw/load_soft_bounds.R`
data("input_config_table", envir = .pkgenv)

#' Fitted GAM object for Qualy's
#'
#' @name qualy_model_obj
#' @format  An object generated by mgcv::bam
#' @source ??
data("qualy_model_obj", envir = .pkgenv)

#' Fitted GAM object for Costs
#'
#' @name cost_model_obj
#' @format  An object generated by mgcv::bam
#' @source ??
data("cost_model_obj", envir = .pkgenv)

#' Relative limits matrix
#'
#' @name relative_limits
#' @format a binary matrix encoding relations between inputs, namely:
#'  `relative_limits[i,j] == 1` implies that input[i] > input[j]
data("relative_limits", envir = .pkgenv)
